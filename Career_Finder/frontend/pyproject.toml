[project]
name = "frontend"
version = "1.0.0"
description = "Career Path Advisor — Frontend"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "jinja2",
    "python-dotenv",
    "python-multipart",
]